{
  "rules": [
    {
      "id": "arch-001",
      "description": "Framework surfaces exist at the repository root",
      "check": "node scripts/framework-check.mjs arch:required-surfaces",
      "expect": "PASS",
      "what": "A required harness surface is missing from the repository root.",
      "why": "The framework only works as a reusable bootstrap when its root instructions, state files, scripts, and docs are discoverable in consistent locations.",
      "fix": "Restore the missing root file or directory, or update the documented framework contract before changing the repository layout."
    },
    {
      "id": "arch-002",
      "description": "Feature tracker preserves WIP=1 and dependency order",
      "check": "node scripts/framework-check.mjs arch:wip-contract",
      "expect": "PASS",
      "what": "feature_list.json allows multiple active features or an active feature with unmet dependencies.",
      "why": "The harness depends on a single active feature and explicit dependency ordering to keep sessions scoped, resumable, and verifiable.",
      "fix": "Set at most one feature to active and ensure its dependencies are already passing before resuming implementation."
    },
    {
      "id": "arch-003",
      "description": "Framework docs are project-specific, not template placeholders",
      "check": "node scripts/framework-check.mjs arch:no-placeholders",
      "expect": "PASS",
      "what": "Core framework docs still contain placeholder template text.",
      "why": "Reusable harness instructions must describe the actual framework contract; placeholder docs create false confidence and mislead downstream adopters.",
      "fix": "Replace template copy in README, docs/PRODUCT.md, and docs/quality-document.md with framework-specific guidance and current state."
    },
    {
      "id": "arch-004",
      "description": "Harness manifest accurately declares reusable and instance surfaces",
      "check": "node scripts/framework-check.mjs manifest",
      "expect": "PASS",
      "what": "The manifest is missing, malformed, or declares surfaces that do not exist in the repository.",
      "why": "The reusability contract depends on a machine-readable classification of CORE, INSTANCE, and optional components; a manifest that drifts from the repo makes upgrades and adoption unpredictable.",
      "fix": "Repair .harness/manifest.json so every declared surface exists and matches the repository layout."
    },
    {
      "id": "arch-005",
      "description": "Every tracked file is classified by the seam manifest (or exempted as product-owned)",
      "check": "node scripts/framework-check.mjs manifest",
      "expect": "PASS",
      "what": "A tracked file is not declared as CORE, INSTANCE, TEMPLATE, or product-owned (productRoots) in the manifest, or is declared twice.",
      "why": "Reusability requires that distribution can answer, for every file, whether it ships as-is, ships as a skeleton, never ships, or belongs to the adopting project. Unclassified files get silently dropped or silently leaked — both break the seam contract.",
      "fix": "Classify the file in .harness/manifest.json (core / instance / templates), or add its parent directory to productRoots if it is product-owned."
    }
  ]
}